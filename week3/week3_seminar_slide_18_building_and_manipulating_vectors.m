% ask the user for a word
word = input("Type in a word: ",'s');

% count the number of characters
charNumber = max(size(word));

% call a character of the word
word(1)

%myIndexes = [4 3 2 1]; % set an order
%myString = 'part'; % string to be re-arranged

% output a random permutation of the letters of the word
